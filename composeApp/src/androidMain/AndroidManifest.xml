<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Single Android entry point lives in :androidApp (MainActivity + MiraSalonApplication).
         composeApp only provides the shared Compose content; it must not declare its own
         launcher activity, otherwise manifest merge would silently shadow the app module. -->
    <application />
</manifest>
